name: Docker Image CI

on:
  release:
    types: [ "released", "prereleased" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . -t hermes:${{ github.ref_name }}
